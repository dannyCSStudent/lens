[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lens-api"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "psycopg2-binary",
  "redis"
]

[tool.setuptools.packages.find]
where = ["."]
